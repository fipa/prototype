<h1>Listing gift_categories</h1>

<table>
  <tr>
    <th>Gift</th>
    <th>Category</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @gift_categories.each do |gift_category| %>
  <tr>
    <td><%= gift_category.gift_id %></td>
    <td><%= gift_category.category_id %></td>
    <td><%= link_to 'Show', gift_category %></td>
    <td><%= link_to 'Edit', edit_gift_category_path(gift_category) %></td>
    <td><%= link_to 'Destroy', gift_category, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Gift category', new_gift_category_path %>
